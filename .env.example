# ============================================================================
# BosqueGracias Environment Variables
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control!
# ============================================================================

# ============ Upstash Redis Configuration ============
# Get these from: https://console.upstash.com/
# âš ï¸ SECURITY: These are SERVER-ONLY variables (no NEXT_PUBLIC_ prefix)
UPSTASH_REDIS_REST_URL=your_upstash_redis_url_here
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token_here

# ============ OnchainKit Configuration ============
# Get these from: https://portal.cdp.coinbase.com/
# âœ… SAFE: These API keys are designed to be public (client-side)
NEXT_PUBLIC_ONCHAINKIT_API_KEY=your_onchainkit_api_key_here
NEXT_PUBLIC_ONCHAINKIT_PROJECT_ID=your_onchainkit_project_id_here

# ============ Blockchain Configuration ============
# Chain ID: 8453 (Base Mainnet) or 84532 (Base Sepolia)
# âœ… SAFE: Chain ID is public information
NEXT_PUBLIC_SDK_CHAIN_ID=8453

# RPC URL (optional - defaults to public RPC)
# âš ï¸ SECURITY: Server-only (no NEXT_PUBLIC_ prefix)
RPC_URL=https://mainnet.base.org

# ============ Smart Contract Address ============
# Update this after deploying your BosqueGracias NFT contract
# âœ… SAFE: Contract addresses are public on the blockchain
NEXT_PUBLIC_BOSQUE_NFT_ADDRESS=0x0000000000000000000000000000000000000000

# ============ Pinata (IPFS) Configuration ============
# Get these from: https://pinata.cloud/
# âš ï¸ SECURITY: JWT should be SERVER-ONLY (use in API routes only)
# Use API routes (e.g., /api/upload) to handle file uploads securely
PINATA_JWT=your_pinata_jwt_here

# âœ… SAFE: Gateway URL is public (read-only access to IPFS)
NEXT_PUBLIC_PINATA_GATEWAY=your_gateway_url_here

# ============ Admin Wallet Address ============
# The wallet address that can perform admin actions
# âš ï¸ SECURITY: Server-only (no NEXT_PUBLIC_ prefix)
ADMIN_WALLET_ADDRESS=your_admin_wallet_address_here

# ============ Application Configuration ============
# âœ… SAFE: These are public configuration values
NEXT_PUBLIC_APP_NAME=BosqueGracias
NEXT_PUBLIC_APP_URL=https://your-app-url.vercel.app

# ============ Farcaster API Key (Optional) ============
# For enhanced user data fetching
# âš ï¸ SECURITY: Server-only (no NEXT_PUBLIC_ prefix)
FARCASTER_API_KEY=NEYNAR_FROG_FM
